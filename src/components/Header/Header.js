import './Header.css'

export const Header = () => {
    return (
        <header className="row">
            <h1>Task Tracker</h1>
            <button className="clickable">Add</button>
        </header>
    )
}